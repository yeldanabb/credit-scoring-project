version: '3.8'

services:
  db:
    image: postgres:15
    container_name: credit_db
    restart: always
    environment:
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=password123
      - POSTGRES_DB=lending_club
      - POSTGRES_HOST_AUTH_METHOD=trust  
    ports:
      - "5432:5432"
    volumes:
      - ./postgres_data:/var/lib/postgresql/data